# AGENT-04 — T5-A2 SEMANTIC SELF-EMBEDDING

This is a mathematical gate. Fail closed.

Target family: a universal semantic counterexample witness of the form required by the repository, conceptually:
forall M : PolyDTMCandidate, exists witness, not (CorrectAt M witness).

Work only through semantically valid self-code / universal-simulation / diagonal-witness constructions already supported by the formalization.

Do NOT reopen the literal fixed-point route G2.1; it is CLOSED_BY_FALSIFICATION.
Do NOT promote finite experiments, circuit samples, heuristic blow-up, clock domination, or empirical evidence into a universal lower bound.

Separate and formalize:
- self-code existence;
- decoding/encoding soundness;
- universal simulation correspondence;
- semantic disagreement versus time-bound failure;
- witness instantiation and quantifier order.

If a kernel-valid proof is achieved, GREEN.
If the remaining statement is genuinely unproved mathematics rather than missing engineering, terminate OPEN_MATH and identify the smallest exact theorem/interface that remains unproved. Never manufacture closure.
