# FINAL_FAIL local nuclei — hardened integration

The proposed topology/operator layer was integrated in a dependency-safe form.

Changes relative to the draft:
- no Mathlib dependency was introduced because the repository currently has no lakefile.toml;
- no sorry was introduced;
- no axiom was substituted for a missing proof;
- the ill-typed RealImaginaryCore placeholder was replaced by a jointly-faithful Re/Im decomposition;
- partial retractions are separated into minus/plus laws;
- retraction implies a compression/retraction fixed point where the law holds;
- seven signatures are instantiated as audit classifications, not endpoint proofs.

The recovered P-vs-NP ThreeTracks/EndpointCompression/StartToMiddle/FinalToMiddle sources remain the strongest concrete prior instance of the start/middle/final geometry and are pinned in FINAL_FAIL_PRIOR_FORMALIZATION_RECOVERY.md.

The signature table is an audit hypothesis layer. It may be refined when exact repository-level compatibility checks expose a mismatch. It does not change FINAL PROOF 0/7.
